services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-agentic_cms_django}
      POSTGRES_USER: ${POSTGRES_USER:-agentic_cms_django}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-agentic_cms_django}
    # Host port 5433 keeps this Postgres off agentic-cms-ts's 5432 (see ../PORTS.md);
    # the in-container port stays 5432 (the web service connects via POSTGRES_HOST=db).
    ports:
      - "${DB_PORT:-5433}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-agentic_cms_django}"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build:
      context: .
      args:
        REQUIREMENTS: dev
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      # Local development only. Do NOT reuse this service definition for staging
      # or production — use config.settings.prod with real secrets there.
      DJANGO_SETTINGS_MODULE: config.settings.dev
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY:-dev-insecure-change-me}
      DJANGO_DEBUG: ${DJANGO_DEBUG:-True}
      DJANGO_ALLOWED_HOSTS: ${DJANGO_ALLOWED_HOSTS:-localhost,127.0.0.1,0.0.0.0}
      POSTGRES_DB: ${POSTGRES_DB:-agentic_cms_django}
      POSTGRES_USER: ${POSTGRES_USER:-agentic_cms_django}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-agentic_cms_django}
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432
      DJANGO_VITE_DEV_MODE: "False"
    volumes:
      - .:/app
      # Keep the image's built assets; don't let the bind mount hide them.
      - /app/frontend/dist
    ports:
      - "${WEB_PORT:-8000}:8000"
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata:
